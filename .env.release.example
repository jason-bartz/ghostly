# Cloudflare R2 credentials for release uploads. Copy to .env.release and fill in.
# .env.release is gitignored. Rotate in Cloudflare if it ever leaks.

R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=ghostly-releases
R2_PUBLIC_BASE=https://downloads.try-ghostly.com
